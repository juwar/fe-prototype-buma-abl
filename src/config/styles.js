import config from './index';

const {fontSize, fontColor} = config;

/**
 * repeatedly configuration (styling) of text
 */
export const textStyles = {
  main: {
    color: fontColor.main,
    // fontSize: fontSize.xxlarge,
    // textAlign: 'center',
    // fontWeight: 'bold',
  },
};
